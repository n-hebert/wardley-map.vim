# Wardley Map Plugin for Vim

# Installation
Copy this shit to `.vim/pack/plugins/opt`. Add `packadd wardley-map.vim` to your vimrc.

# Usage
Edit a `.owm` file. To render:

```
:RenderWardleyMap
```

